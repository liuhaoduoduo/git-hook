# 编译命令配置文件
# 
# 说明：
#   - 每行一条编译命令
#   - 以 # 开头的行为注释，空行会被跳过
#   - 命令按照*顺序执行*，前一条失败会停止后续命令
#   - 支持完整的 shell 语法（管道、&&、||、变量等）
#
# 修改此文件后，下一次触发 git hook（如 pull、merge、checkout）时生效
# 不需要重启任何进程，也不需要修改脚本本身

# 服务1
goenv shell 1.19.13 && go build ./main.go

